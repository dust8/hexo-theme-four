<header>
  <h1><%= config.title %></h1>
  <nav>
    <ul class="menu">
        <% for (name in config.menu||theme.menu) { %>
          <li class="menu__item">
            <a href="<%- url_for(config.menu[name]||theme.menu[name]) %>"><%= name %></a>
          </li>
        <% } %>
      </ul>
  </nav>
</header>
